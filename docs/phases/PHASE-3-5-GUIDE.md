# 🔧 Phase 3: 핵심 기능 (Day 6-12)

> **목표**: 간병인/보호자 대시보드 및 구인구직 핵심 기능 구현
> **예상 소요**: 7일
> **담당**: @frontend, @backend, @database

---

## 📋 체크리스트

- [ ] 3.1 간병인 대시보드
- [ ] 3.2 보호자 대시보드
- [ ] 3.3 구인글 CRUD
- [ ] 3.4 지원 시스템
- [ ] 3.5 프로필 관리
- [ ] 3.6 리뷰 시스템

---

## 3.1 간병인 대시보드

### 페이지 구조
```
/caregiver
├── index.tsx           # 대시보드 (요약)
├── jobs/
│   ├── index.tsx       # 일자리 목록
│   └── [id].tsx        # 일자리 상세
├── applications.tsx    # 지원 현황
└── profile.tsx         # 내 프로필
```

### 대시보드 컴포넌트
- 지원 현황 요약
- 추천 일자리
- 새 메시지 알림
- 평균 평점

---

## 3.2 보호자 대시보드

### 페이지 구조
```
/guardian
├── index.tsx           # 대시보드 (요약)
├── postings/
│   ├── index.tsx       # 내 구인글
│   ├── new.tsx         # 새 구인글
│   └── [id]/
│       ├── index.tsx   # 구인글 상세
│       └── edit.tsx    # 수정
├── caregivers.tsx      # 간병인 검색
└── profile.tsx         # 내 프로필
```

### 대시보드 컴포넌트
- 내 구인글 현황
- 새 지원자 알림
- 진행 중인 채팅
- 최근 리뷰

---

## 3.3 구인글 CRUD

### API 엔드포인트
| Method | Path | 설명 |
|--------|------|------|
| GET | /api/jobs | 목록 조회 |
| POST | /api/jobs | 생성 |
| GET | /api/jobs/[id] | 상세 조회 |
| PUT | /api/jobs/[id] | 수정 |
| DELETE | /api/jobs/[id] | 삭제 |
| POST | /api/jobs/[id]/apply | 지원 |

### 구인글 폼 필드
- 제목
- 상세 설명
- 근무지 (주소)
- 급여 (시급/일급/월급)
- 근무 시간
- 환자 정보 (성별, 나이, 질환)
- 시작일/종료일

---

## 3.4 지원 시스템

### 지원 상태 흐름
```
pending → reviewing → accepted/rejected
                   ↓
               withdrawn
```

### 기능
- 지원서 작성 (자기소개, 희망 급여, 가능 시작일)
- 지원 취소
- 지원 상태 변경 (보호자)
- 알림 발송

---

## 3.5 프로필 관리

### 간병인 프로필
- 기본 정보 (이름, 연락처, 주소)
- 경력 사항
- 자격증 (이미지 업로드)
- 자기소개
- 희망 근무 조건

### 보호자 프로필
- 기본 정보
- 환자와의 관계

---

## 3.6 리뷰 시스템

### 기능
- 근무 완료 후 양방향 리뷰
- 별점 (1-5)
- 세부 평가 (간병인: 성실함, 전문성, 친절함)
- 평균 평점 계산

---

# 💬 Phase 4: 채팅 시스템 (Day 13-16)

> **목표**: Supabase Realtime 기반 실시간 채팅 구현
> **예상 소요**: 4일

---

## 📋 체크리스트

- [ ] 4.1 Supabase Realtime 설정
- [ ] 4.2 채팅방 목록
- [ ] 4.3 채팅 UI
- [ ] 4.4 실시간 메시지
- [ ] 4.5 읽음 표시

---

## 4.1 Supabase Realtime 설정

```typescript
// hooks/useChat.ts
import { useEffect, useState } from 'react'
import { supabase } from '@/lib/supabase'
import type { Message } from '@/types'

export function useChat(roomId: string) {
  const [messages, setMessages] = useState<Message[]>([])
  const [isLoading, setIsLoading] = useState(true)

  useEffect(() => {
    // 기존 메시지 로드
    const loadMessages = async () => {
      const { data } = await supabase
        .from('messages')
        .select('*')
        .eq('room_id', roomId)
        .order('created_at', { ascending: true })
      
      setMessages(data ?? [])
      setIsLoading(false)
    }

    loadMessages()

    // 실시간 구독
    const channel = supabase
      .channel(`room:${roomId}`)
      .on(
        'postgres_changes',
        {
          event: 'INSERT',
          schema: 'public',
          table: 'messages',
          filter: `room_id=eq.${roomId}`,
        },
        (payload) => {
          setMessages((prev) => [...prev, payload.new as Message])
        }
      )
      .subscribe()

    return () => {
      supabase.removeChannel(channel)
    }
  }, [roomId])

  const sendMessage = async (content: string) => {
    await supabase.from('messages').insert({
      room_id: roomId,
      content,
      message_type: 'text',
    })
  }

  return { messages, isLoading, sendMessage }
}
```

---

## 4.2-4.5 채팅 UI

### 페이지 구조
```
/chat
├── index.tsx     # 채팅방 목록
└── [id].tsx      # 채팅방
```

### 주요 컴포넌트
- `ChatList` - 채팅방 목록
- `ChatRoom` - 채팅방 UI
- `MessageBubble` - 메시지 버블
- `MessageInput` - 메시지 입력

---

# 🚀 Phase 5: 배포 및 최적화 (Day 17-20)

> **목표**: 프로덕션 배포 및 성능/접근성 최적화
> **예상 소요**: 4일

---

## 📋 체크리스트

- [ ] 5.1 Kakao Alimtalk 연동
- [ ] 5.2 성능 최적화
- [ ] 5.3 접근성 테스트
- [ ] 5.4 Vercel 배포
- [ ] 5.5 모니터링 설정

---

## 5.1 Kakao Alimtalk 연동

### 알림 템플릿
| 코드 | 이름 | 발송 시점 |
|------|------|----------|
| CM_001 | 지원서 접수 | 새 지원 시 (보호자에게) |
| CM_002 | 지원 승인 | 지원 승인 시 (간병인에게) |
| CM_003 | 지원 거절 | 지원 거절 시 (간병인에게) |
| CM_004 | 새 메시지 | 채팅 메시지 시 (상대방에게) |
| CM_005 | 매칭 완료 | 매칭 확정 시 (양쪽에게) |

---

## 5.2 성능 최적화

### Core Web Vitals 목표
| 지표 | 목표 |
|------|------|
| LCP | < 2.5s |
| FID | < 100ms |
| CLS | < 0.1 |

### 최적화 전략
- 이미지 최적화 (next/image)
- 코드 스플리팅
- API 캐싱
- 폰트 최적화

---

## 5.3 접근성 테스트

### 테스트 도구
- Lighthouse Accessibility
- axe DevTools
- 실제 고령자 사용자 테스트

### 체크포인트
- [ ] 모든 텍스트 16px 이상
- [ ] 모든 버튼 48px 이상
- [ ] 색상 대비 4.5:1 이상
- [ ] 키보드 네비게이션
- [ ] 스크린 리더 호환

---

## 5.4 Vercel 배포

```bash
# Vercel CLI 설치
npm i -g vercel

# 배포
vercel

# 프로덕션 배포
vercel --prod
```

### 환경 변수 설정
Vercel 대시보드에서 모든 환경 변수 설정

---

## 5.5 모니터링

### 설정
- Vercel Analytics
- Supabase Dashboard
- Error tracking (Sentry)

---

## ✅ 최종 체크리스트

- [ ] 모든 기능 정상 작동
- [ ] 접근성 점수 90점 이상
- [ ] 성능 점수 80점 이상
- [ ] 보안 취약점 없음
- [ ] 문서화 완료

---

*Phase 3-5 v1.0*
